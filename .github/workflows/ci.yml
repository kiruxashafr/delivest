name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  ci:
    name: Delivest - lint, typecheck, tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        ports:
          - 5432:5432
        env:
          POSTGRES_DB: myapp
          POSTGRES_USER: myuser
          POSTGRES_PASSWORD: 1234567
        options: >-
          --health-cmd="pg_isready -U test"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Generate prisma client
        env:
          DATABASE_URL: "postgresql://user:pass@localhost:5432/dummy"
        run: pnpm prisma:generate

      - name: Build Libraries (Types & Common)
        run: |
          pnpm build:types
          pnpm build:common

      - name: Audit
        run: pnpm audit --production || true

      - name: Format Check
        run: pnpm format:check

      - name: Lint All
        run: pnpm lint

      - name: Test All
        run: pnpm test
